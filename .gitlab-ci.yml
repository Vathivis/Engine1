stages:
    - build
    
image: gcc
    
before_script:
    # Install build dependencies
    - apt-get update
    
build:
    stage: build
    script:
        - cd vendor/bin/premake
        - ./premake5-linux